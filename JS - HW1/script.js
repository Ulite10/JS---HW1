'use strict'

const firstNum = prompt('Please enter any number', 0);
const secondNum = prompt('Please enter one more number', 0);
const result = Number(firstNum) + Number(secondNum)
const show = alert(String(firstNum) + '+' + String(secondNum) + '=' + result);